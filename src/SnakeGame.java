public class SnakeGame {

   public static void main(String[] args){

      /*Should be :
      GameFrame frame = new GameFrame();
      But since we've already created a GameFrame file, so if we don't plan to use the frame instance again,
       "new GameFrame()" would also works.
       */
      new GameFrame();
   }
}
